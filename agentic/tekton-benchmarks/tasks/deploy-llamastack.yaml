apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: deploy-llamastack
  namespace: tekton-llamastack
spec:
  description: |
    Deploy LlamaStackDistribution using the RHOAI operator-managed rh-dev distribution.
    Requires the llamastackoperator to be enabled (Managed) in the DataScienceCluster.
    Waits for the distribution to be ready before completing.
  params:
    - name: NAMESPACE
      description: Namespace to deploy LlamaStack
      type: string
      default: "llamastack"
    - name: MANIFEST_PATH
      description: Path to LlamaStackDistribution manifest
      type: string
      default: "https://raw.githubusercontent.com/openshift-psap/llamastack-performance/main/agentic/tekton-benchmarks/manifests/llamastack.yaml"
    - name: DISTRIBUTION_NAME
      description: Name of the LlamaStackDistribution
      type: string
      default: "llamastack-benchmark"
    - name: VLLM_NAMESPACE
      description: Namespace where vLLM is running (for cross-namespace reference)
      type: string
      default: ""
    - name: MODEL_NAME
      description: Name of the vLLM model/InferenceService (used to construct predictor service name)
      type: string
      default: "llama-32-3b-instruct"
    - name: SKIP_DEPLOY
      description: If "true", skips deployment
      type: string
      default: "false"
    - name: DISABLE_OTEL
      description: If "true", disables OpenTelemetry in LlamaStack (sets OTEL_SDK_DISABLED=true, removes collector env vars)
      type: string
      default: "false"
    - name: REPLICAS
      description: Number of LlamaStack replicas
      type: string
      default: "1"
    - name: ENABLE_HPA
      description: "If 'true', creates a HorizontalPodAutoscaler for LlamaStack"
      type: string
      default: "false"
    - name: HPA_MIN_REPLICAS
      description: Minimum replicas for HPA
      type: string
      default: "1"
    - name: HPA_MAX_REPLICAS
      description: Maximum replicas for HPA
      type: string
      default: "4"
    - name: HPA_MEMORY_TARGET
      description: Target memory utilization percentage for HPA scaling
      type: string
      default: "75"
  steps:
    - name: deploy-llamastack
      image: quay.io/openshift/origin-cli:latest
      script: |
        #!/bin/bash
        set -e
        
        if [ "$(params.SKIP_DEPLOY)" = "true" ]; then
          echo "SKIP_DEPLOY is true, skipping LlamaStack deployment."
          exit 0
        fi
        
        echo "=============================================="
        echo "Deploying LlamaStack (RHOAI operator rh-dev)"
        echo "=============================================="
        echo "Namespace: $(params.NAMESPACE)"
        echo "Distribution: $(params.DISTRIBUTION_NAME)"
        echo ""
        
        # Verify the LlamaStack operator is running
        echo "Checking LlamaStack operator status..."
        if ! oc get deployment -n redhat-ods-applications -l app.kubernetes.io/name=llama-stack-k8s-operator -o name 2>/dev/null | grep -q deployment; then
          echo "WARNING: LlamaStack operator not found. Ensure llamastackoperator is set to Managed in the DSC."
          echo "Attempting to enable it..."
          oc patch datasciencecluster default-dsc --type merge -p '{"spec":{"components":{"llamastackoperator":{"managementState":"Managed"}}}}' || true
          echo "Waiting for operator to start..."
          sleep 30
        fi
        
        # Delete existing LlamaStack if present
        echo "Cleaning up existing LlamaStack..."
        oc delete llamastackdistribution $(params.DISTRIBUTION_NAME) -n $(params.NAMESPACE) --ignore-not-found=true
        
        echo "Waiting for cleanup..."
        sleep 10
        
        MODEL_PREDICTOR="$(params.MODEL_NAME)-predictor"
        VLLM_NS="$(params.VLLM_NAMESPACE)"
        DISABLE_OTEL="$(params.DISABLE_OTEL)"
        echo "Model predictor: $MODEL_PREDICTOR"
        echo "Disable OTel: $DISABLE_OTEL"

        MANIFEST=$(curl -sL $(params.MANIFEST_PATH))

        # Patch VLLM_URL for model name and optional cross-namespace
        if [ -n "$VLLM_NS" ] && [ "$VLLM_NS" != "$(params.NAMESPACE)" ]; then
          echo "Patching VLLM_URL for cross-namespace access (vLLM in $VLLM_NS)..."
          MANIFEST=$(echo "$MANIFEST" | \
            sed "s|http://llama-32-3b-instruct-predictor:80/v1|http://${MODEL_PREDICTOR}.${VLLM_NS}.svc.cluster.local:80/v1|g")
        else
          MANIFEST=$(echo "$MANIFEST" | \
            sed "s|http://llama-32-3b-instruct-predictor:80/v1|http://${MODEL_PREDICTOR}:80/v1|g")
        fi

        MANIFEST=$(echo "$MANIFEST" | \
          sed "s|value: \"llama-32-3b-instruct\"|value: \"$(params.MODEL_NAME)\"|g" | \
          sed "s|NAMESPACE_PLACEHOLDER|$(params.NAMESPACE)|g")

        # If DISABLE_OTEL=true, strip OTel env vars and inject OTEL_SDK_DISABLED=true
        if [ "$DISABLE_OTEL" = "true" ]; then
          echo "Disabling OpenTelemetry — removing collector env vars, setting OTEL_SDK_DISABLED=true"
          MANIFEST=$(echo "$MANIFEST" | \
            sed '/- name: OTEL_EXPORTER_OTLP_ENDPOINT/{N;d;}' | \
            sed '/- name: OTEL_EXPORTER_OTLP_PROTOCOL/{N;d;}' | \
            sed '/- name: OTEL_SERVICE_NAME/{N;d;}' | \
            sed '/- name: OTEL_RESOURCE_ATTRIBUTES/{N;d;}')
          MANIFEST=$(echo "$MANIFEST" | \
            sed '/- name: POSTGRES_HOST/i\        - name: OTEL_SDK_DISABLED\n          value: "true"')
        fi

        # Patch replica count
        REPLICAS="$(params.REPLICAS)"
        if [ "$REPLICAS" != "1" ]; then
          echo "Setting replicas to $REPLICAS"
          MANIFEST=$(echo "$MANIFEST" | sed "s|replicas: 1|replicas: $REPLICAS|g")
        fi

        echo "Deploying LlamaStackDistribution..."
        echo "$MANIFEST" | oc apply -f - -n $(params.NAMESPACE)
        
        # Wait for LlamaStack to be ready
        echo "Waiting for LlamaStack to be ready..."
        for i in {1..60}; do
          PHASE=$(oc get llamastackdistribution $(params.DISTRIBUTION_NAME) -n $(params.NAMESPACE) -o jsonpath='{.status.phase}' 2>/dev/null || echo "NotFound")
          echo "  Status: $PHASE"
          if [ "$PHASE" = "Ready" ]; then
            VERSION=$(oc get llamastackdistribution $(params.DISTRIBUTION_NAME) -n $(params.NAMESPACE) -o jsonpath='{.status.version.llamaStackServerVersion}' 2>/dev/null || echo "unknown")
            OPERATOR_VER=$(oc get llamastackdistribution $(params.DISTRIBUTION_NAME) -n $(params.NAMESPACE) -o jsonpath='{.status.version.operatorVersion}' 2>/dev/null || echo "unknown")
            echo ""
            echo "=============================================="
            echo "LlamaStack deployed successfully!"
            echo "  Server version:   $VERSION"
            echo "  Operator version: $OPERATOR_VER"
            echo "  Distribution:     rh-dev (RHOAI managed)"
            echo "=============================================="

            # Pin LlamaStack pods to the GPU worker node (co-locate with vLLM)
            DEPLOY_NAME=$(oc get deployment -n $(params.NAMESPACE) -l app=llama-stack -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || echo "")
            if [ -n "$DEPLOY_NAME" ]; then
              GPU_NODE=$(oc get nodes -l nvidia.com/gpu.present=true -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || echo "")
              if [ -n "$GPU_NODE" ]; then
                echo "Pinning LlamaStack ($DEPLOY_NAME) to GPU node: $GPU_NODE"
                oc patch deployment "$DEPLOY_NAME" -n $(params.NAMESPACE) --type merge \
                  -p "{\"spec\":{\"template\":{\"spec\":{\"nodeSelector\":{\"kubernetes.io/hostname\":\"$GPU_NODE\"}}}}}" || true
                echo "Waiting for rollout..."
                oc rollout status deployment/"$DEPLOY_NAME" -n $(params.NAMESPACE) --timeout=120s || true
              fi
            fi

            # Create HPA if enabled
            ENABLE_HPA="$(params.ENABLE_HPA)"
            if [ "$ENABLE_HPA" = "true" ] && [ -n "$DEPLOY_NAME" ]; then
              HPA_MIN="$(params.HPA_MIN_REPLICAS)"
              HPA_MAX="$(params.HPA_MAX_REPLICAS)"
              HPA_MEM="$(params.HPA_MEMORY_TARGET)"
              echo ""
              echo "Creating HPA for $DEPLOY_NAME (memory target: ${HPA_MEM}%, min: $HPA_MIN, max: $HPA_MAX)"

              cat <<HPAEOF | oc apply -f - -n $(params.NAMESPACE)
        apiVersion: autoscaling/v2
        kind: HorizontalPodAutoscaler
        metadata:
          name: $(params.DISTRIBUTION_NAME)-hpa
        spec:
          scaleTargetRef:
            apiVersion: apps/v1
            kind: Deployment
            name: $DEPLOY_NAME
          minReplicas: $HPA_MIN
          maxReplicas: $HPA_MAX
          metrics:
            - type: Resource
              resource:
                name: memory
                target:
                  type: Utilization
                  averageUtilization: $HPA_MEM
        HPAEOF

              echo "HPA created:"
              oc get hpa $(params.DISTRIBUTION_NAME)-hpa -n $(params.NAMESPACE)
            fi

            exit 0
          fi
          sleep 5
        done
        
        echo "ERROR: LlamaStack did not become ready in time"
        echo "Status details:"
        oc get llamastackdistribution $(params.DISTRIBUTION_NAME) -n $(params.NAMESPACE) -o jsonpath='{.status.conditions[0].message}' 2>/dev/null || true
        exit 1
